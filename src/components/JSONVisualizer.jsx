import React from "react";

const JSONVisualizer = () => {
  return (
    <div className="json-visualizer">
      <h3>JSON Data Viewer</h3>
      <p>We’ll map the file structure here.</p>
    </div>
  );
};

export default JSONVisualizer;
