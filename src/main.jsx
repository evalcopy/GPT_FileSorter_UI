import React from "react";
import ReactDOM from "react-dom/client";
import UIFramework from "./UIFramework.jsx";

ReactDOM.createRoot(document.getElementById("root")).render(
  <React.StrictMode>
    <UIFramework />
  </React.StrictMode>
);
